import 'package:flutter/material.dart';

class PublisherAddBanner extends StatefulWidget {
  const PublisherAddBanner({Key? key}) : super(key: key);

  @override
  State<PublisherAddBanner> createState() => _PublisherAddBannerState();
}

class _PublisherAddBannerState extends State<PublisherAddBanner> {
  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
